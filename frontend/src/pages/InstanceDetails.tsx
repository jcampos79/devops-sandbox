// Placeholder instance detail/terminal page. Full instance metadata display
// lands in Phase 4; the embedded xterm.js terminal (ticket-authenticated
// WebSocket -> Kubernetes exec) lands in Phase 5 -- see README Roadmap and
// spec Sections 17/19.

export default function InstanceDetails() {
  return (
    <div>
      <h1>Instance</h1>
      <p>Instance detail view and terminal are coming in Phases 4-5.</p>
    </div>
  );
}
