// Placeholder login page. Local-account auth (username/password against
// the FastAPI backend) lands in Phase 2 -- see README Roadmap.

export default function Login() {
  return (
    <div>
      <h1>Log in</h1>
      <p>Authentication is coming in Phase 2.</p>
    </div>
  );
}
