---
# Copy to secrets.yml (or better: ansible-vault encrypt it in place) and
# fill in real values. secrets.yml is git-ignored; never commit real
# credentials.

postgres_password: "changeme"
backend_secret_key: "changeme-generate-a-long-random-value"
